import project from './project-schema'
import page from './page-schema'
import skills from './skills-schema';
import embed from './embed-schema';

const schemas = [project, page, skills, embed];

export default schemas;